# Verification — 0.0.5

Checked on 2026-09-25 in the supplied execution environment using Python 3.13.5. No real PBS server, MQTT broker, or SMTP service was contacted.

## Baseline inspection

- Extract and inspect the complete supplied 0.0.4 ZIP before changing code: launcher, all six `pbs_maintenance` package files, both TOML files, README, code map, version history, command reference, tests, requirements, verification notes, ignore rules, and release manifest.
- Run the untouched 0.0.4 test suite: all 30 offline regression/integration tests pass.
- Compile the untouched launcher, package modules, and test module successfully with bytecode redirected outside the project.
- Run untouched `--help` and `--version`; both exit before configuration/logging/runtime work.
- Treat the supplied 0.0.4 archive as the immediate baseline. The older unversioned source archive referenced by the inherited manifest was not supplied in this conversation, so its historical SHA-256 is retained from the prior manifest rather than independently re-read.

## 0.0.5 change verification

- Keep maintenance logic unchanged. The only production-code edit outside version metadata is CLI help wording in the existing launcher parser; command planning, config schema/defaults, validation, logging, SMTP, MQTT, subprocess execution, payloads, step ordering, and failure-stop behavior are unchanged.
- Confirm both version markers are 0.0.5 and update all current-version headers/references in the README, TOML examples, command reference, code map, tests, verification file, and release manifest.
- Strengthen the existing help/version regression test to require the documented CLI semantics: operational settings remain in TOML; relative `--config` paths use the current working directory; logs remain beside the launcher; help/version perform no runtime setup.
- Confirm `config-example.toml` still parses to exactly `settings.DEFAULT_CONFIG`, including all existing options and safe default `dry_run.enabled = true` with dry-run notification sends disabled.
- Confirm the user-provided disclaimer remains present in README with the same wording; no project-name or link substitution was needed inside the disclaimer itself.
- Compare the final release tree with 0.0.4 and account for every changed path; retain all prior project paths and add no production file or dependency.

## Regression and integration checks

Run the complete offline suite against 0.0.5: **all 30 tests pass**. The tests cover fixed sync → verify → prune → GC order, stop-on-first-command-failure behavior, manual-retention guards/forwarding, disabled-step payload fields, dry-run non-execution, independent MQTT/email sends, notification failures, launch failure handling, full/error logging behavior, config/parser validation, CA path handling, active dependency checks, SMTP security modes/refusals, error classification, launcher path resolution, simulated cross-module CLI execution, and import-side-effect prevention.

Run these additional release checks:

- Python compilation for the launcher, all package modules, and tests with generated bytecode outside the project.
- Direct `--help` and `--version` checks from the release tree.
- AST-based inventory of manually defined functions/classes compared with `commented_code_map.md` to ensure every definition remains documented.
- TOML schema/default parity and documentation-key coverage checks.
- Disclaimer presence/wording check.
- ZIP CRC test, duplicate-member/path safety checks, manifest hash verification, baseline-path preservation, and generated-artifact exclusion checks.
- Extract the final ZIP to a clean directory and rerun compile, help/version, and the full 30-test suite from that extracted copy.

## Package verification

The packaged 0.0.5 ZIP contains the complete project beneath one versioned top-level directory: 19 files total, with all 18 non-manifest files covered by `RELEASE_MANIFEST.json`. ZIP member parity, path safety, CRC integrity, baseline-path preservation, and all manifest file hashes pass. No `__pycache__`, `.pyc`, `.pyo`, runtime `logs/`, virtual environments, build caches, or temporary files are included. A clean extraction compiles successfully, reports version 0.0.5, and passes all 30 offline tests. The manifest excludes its own self-hash to avoid recursion; the external ZIP SHA-256 and ZIP CRC cover the complete archive.

## Limits

Real PBS command compatibility and datastore effects were not tested. No real prune or garbage-collection operation was performed. MQTT/SMTP delivery, production scheduling, concurrency, long-running output, process interruption, full-disk behavior, and permissions on an actual PBS host remain untested. Existing application limitations remain unchanged: no overlap lock, PBS subprocess timeout, rollback, or independent PBS task polling; captured subprocess output is unbounded in memory; pipe-reader joins use the existing five-second timeout; and unexpected failures can still end without an outcome notification. Review README.md and the source before production use.
